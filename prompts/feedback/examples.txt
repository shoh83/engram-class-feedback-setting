Include category-specific improvement examples only when they are absolutely needed to make the feedback clearer, more concrete, or more persuasive.
If the category comment already stands well on its own, omit `exampleCase`.
Choose `exampleCase` only from the provided edit snippets.
Do not invent a new original/revised pair or synthesize a broader rewrite.
If there is no edit snippet that directly supports the category comment, omit `exampleCase`.
For `과제 수행`, `의미 전달`, and `구조`, rely on the diff between the original text and the rewritten version.
For `어휘 및 표현` and `문법 정확성`, prefer edit snippets from the original text to the minimally corrected version unless the rewritten diff is the only direct support.
Each example must show:
- an original phrase or sentence
- a revised phrase or sentence
- why the original-to-revised change justifies the feedback
Use the selected diff data to show how the writing could become clearer, more natural, or better organized.
Each `exampleCase` must be tied to the same category's `comment`, not added as a generic extra example.
The original/revised pair should directly support the specific problem and advice described in that category comment.
